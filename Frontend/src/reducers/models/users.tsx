import {Model} from '../../models/models';
import UserInterface = Model.UserInterface;

export default function() {
}

export const allUsersReducer = (): UserInterface[] => {
    return [
        {}
    ];
};

export const activeUserReducer = (): UserInterface[] => {
    return [
        {}
    ];
};

